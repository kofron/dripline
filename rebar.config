% -*- erlang -*-

% compiler options
{erl_opts, [debug_info, {src_dirs, ["src"]}]}.

% dependency directory where things will live.  we use the erlang
% lib/ standard.
{deps_dir, ["lib"]}.

{deps,
  [
    {couchbeam,
      "0.8.*",
      {git,"git://github.com/kofron/couchbeam.git", "HEAD"}
    }
  ]
}.